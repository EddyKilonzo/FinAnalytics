{
  "name": "finanalytics",
  "version": "1.0.0",
  "private": true,
  "description": "FinAnalytix — backend + ML service run scripts",
  "scripts": {
    "start:ml": "cd ml-service && python -m uvicorn main:app --reload --port 8000",
    "start:backend": "cd backend && npm run start:dev",
    "start:stack": "concurrently -n ml,api -c blue,green \"npm run start:ml\" \"npm run start:backend\"",
    "install:all": "npm install && cd backend && npm install && cd ../frontend && npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
